<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&display=swap">
    <title>AEB Files</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Abril+Fatface&family=Poppins&display=swap');
        @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;600;700;800;900&display=swap');
        body{
            /* padding: 0; */
            /* margin: 0; */
            background-color: black;

        }
        .carrossel{
            display: flex;
            margin-top: 1%;
            height: 50vh;
            background-color: white;
            border-radius: 20px;
        }
        .navegador{
            display: flex;
            justify-content: space-between;
            padding: 2% 5%;
            font-family: Orbitron, sans-serif;
            font-weight: bold;
            font-size: 12px;
        }
        a {
            color: white;
            text-transform: uppercase;
            text-decoration: none;
        }
        .barra_de_pesquisa{
            display: flex;
            justify-content: center;

        }
        .pastas {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            width: 100%;
            margin: 0;
            text-transform: uppercase;
            text-decoration: none;
            font-weight: bold;
            font-size: 12px;
        }

        .card {
            width: 25%;
            height: auto;
            display: flex;
            flex-direction: column;
            margin:10px 2% 0% 2%;
            border-radius: 20px;
            background-color: white;
        }

        .foto {
            display: flex;
            margin: 10px 10px 0px ;
            height: 100px;
            text-align: center;
            background-color: rgb(56, 8, 228);
            border-radius: 20px;
            overflow: hidden;
            font-family: Orbitron, sans-serif;
        }
        .foto img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .card div:last-child {
            flex: 0;
            text-align: center;
            padding: 10px;
        }
    </style>
</head>
<body>
    <header>
        <nav class="navegador">
            <div>
                <a href="index.php">Logo</a>
            </div>
            <div>
                <a href="index.php">AEB Files</a>
            </div>
            <div>
                <a href="index.php">Opções</a>
            </div>
        </nav>
    </header>
    <main>
        <section class="carrossel">
            <p> Carrossel de Imagens Famosas</p>
        </section>
        <section class="barra_de_pesquisa">
            
            <form action="/action_page.php">
                <input type="search" id="gsearch" name="gsearch">
                <input type="submit" value="Submit">
            </form>

        </section>
        <section class="pastas">
            <h2>Pastas:</h2>
            <ul>
                {% for folder in folders %}
                    <li><a href="{{ url_for('show_folder', folder_name=folder) }}">{{ folder }}</a></li>
                {% endfor %}
            </ul>

            {% for folder in folders %}
                <a href="{{ url_for('show_folder', folder_name=folder) }}" class="card">
                    <div class="foto">
                        {% if folder_images[folder] %}
                            {% set first_image_path = url_for('view_image', folder_name=folder, image_name=folder_images[folder][0]) %}
                            <img src="{{ first_image_path }}" alt="First Image">
                        {% else %}
                            <!-- If no images, you can provide a default image or leave it empty -->
                        {% endif %}
                    </div>
                    <div>
                        <p>{{ folder }}</p>
                    </div>
                </a>
            {% endfor %}

        </section>
    </main>
    <footer>

    </footer>
</body>
</html>